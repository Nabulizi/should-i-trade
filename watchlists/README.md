# Watchlists

Put TradingView watchlist exports in this folder.

The dashboard reads `Watchlist_Nur.txt` by default. TradingView exports are local
trading context, so `*.txt` files in this folder are intentionally ignored by Git.
